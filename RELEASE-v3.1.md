# Legacy Protocol v3.1 — Main Navigation Centering Fix

- Main navigation now scrolls horizontally to center the active tab.
- Applies when opening Check-in or any other main section from Home or internal calls to `showPanel()`.
- No vertical recentering is introduced.
- Client data, program files, offline support, and update logic are unchanged.
